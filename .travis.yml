language: node_js
dist: bionic
node_js:
  - 10
  - 12
cache:
  directories:
    - node_modules
  
install:
  - npm install
  - npm install -g codecov

script:
  - npm run test

after_success:
  - if [[ $TRAVIS_NODE_VERSION == 10 ]]; then codecov; fi
